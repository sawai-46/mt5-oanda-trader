diff --git a/python/inference_server_7module.py b/python/inference_server_7module.py
index 8bf1f7f..ec97a8d 100644
--- a/python/inference_server_7module.py
+++ b/python/inference_server_7module.py
@@ -1498,14 +1498,22 @@ ATR: {atr}
         confidence = 0.5
         reason = response[:100]
         
-        response_upper = response.upper()
-        
-        if "BUY" in response_upper:
-            signal = 1
-        elif "SELL" in response_upper:
-            signal = -1
-        elif "WAIT" in response_upper:
-            signal = 0
+        # 判定部分を正規表現で厳密に抽出（理由文の"BUY"等に誤爆しないように）
+        match = re.search(r'判定[:：\s]*(BUY|SELL|WAIT)', response.upper())
+        if match:
+            decision = match.group(1)
+            if decision == "BUY":
+                signal = 1
+            elif decision == "SELL":
+                signal = -1
+            else:
+                signal = 0
+        else:
+            first_word = response.strip().split()[0].upper() if response.strip() else ""
+            if first_word == "BUY":
+                signal = 1
+            elif first_word == "SELL":
+                signal = -1
         
         conf_match = re.search(r'確信度[:\s]*([0-9.]+)', response)
         if conf_match:
diff --git a/python/inference_server_hybrid.py b/python/inference_server_hybrid.py
index f614f98..74f95a5 100644
--- a/python/inference_server_hybrid.py
+++ b/python/inference_server_hybrid.py
@@ -305,14 +305,22 @@ ATR: {atr}
         confidence = 0.5
         reason = response[:100]
         
-        response_upper = response.upper()
-        
-        if "BUY" in response_upper:
-            signal = 1
-        elif "SELL" in response_upper:
-            signal = -1
-        elif "WAIT" in response_upper:
-            signal = 0
+        # 判定部分を正規表現で厳密に抽出（理由文の"BUY"等に誤爆しないように）
+        match = re.search(r'判定[:：\s]*(BUY|SELL|WAIT)', response.upper())
+        if match:
+            decision = match.group(1)
+            if decision == "BUY":
+                signal = 1
+            elif decision == "SELL":
+                signal = -1
+            else:
+                signal = 0
+        else:
+            first_word = response.strip().split()[0].upper() if response.strip() else ""
+            if first_word == "BUY":
+                signal = 1
+            elif first_word == "SELL":
+                signal = -1
         
         conf_match = re.search(r'確信度[:\s]*([0-9.]+)', response)
         if conf_match:
diff --git a/python/inference_server_lm.py b/python/inference_server_lm.py
index 52474ef..bfce14f 100644
--- a/python/inference_server_lm.py
+++ b/python/inference_server_lm.py
@@ -138,18 +138,25 @@ ATR: {atr}
         confidence = 0.5
         reason = response
         
-        response_upper = response.upper()
-        
-        # シグナル判定
-        if "BUY" in response_upper:
-            signal = 1
-        elif "SELL" in response_upper:
-            signal = -1
-        elif "WAIT" in response_upper:
-            signal = 0
-        
-        # 確信度抽出（簡易版）
+        # 判定部分を正規表現で厳密に抽出（理由文の"BUY"等に誤爆しないように）
         import re
+        match = re.search(r'判定[:：\s]*(BUY|SELL|WAIT)', response.upper())
+        if match:
+            decision = match.group(1)
+            if decision == "BUY":
+                signal = 1
+            elif decision == "SELL":
+                signal = -1
+            else:
+                signal = 0
+        else:
+            first_word = response.strip().split()[0].upper() if response.strip() else ""
+            if first_word == "BUY":
+                signal = 1
+            elif first_word == "SELL":
+                signal = -1
+        
+        # 確信度抽出
         conf_match = re.search(r'確信度[:\s]*([0-9.]+)', response)
         if conf_match:
             try:
